function [x,it,fx,ttot,fh,timeVec,gnrit] = BCGD_rand(y_un,b)
%BCGD_RAND La funzione implementa il BCGD method con randomized block
%coordinate

%input: 
%b = numero di blocchi in cui divido le variabili
%y_un variabile per cui minimizzo










end

